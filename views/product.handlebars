<div>
    <img src="../img/{{product1.file1}}" alt="" class="">
    <h1>{{product1.title}}</h1>
    <p>{{product1.price}}</p>
    <ul>
        {{#each product}}
            <li><a class="" href="/color/{{this.id}}"><div style="display: inline-block; width: 50px; height: 50px;;background-color: {{this.cor}};"></div></a></li>
        {{/each}}
    </ul>

    <form action="/products/remove/{{product1.id}}" method="post">
        <input type="submit" value="Remover">
    </form>
    <a href="/insert-cor/{{product1.id}}">editar</a>
</div>